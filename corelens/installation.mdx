---
title: "Installation"
description: "Install Corelens and optional framework dependencies for Express, Fastify, and Hono adapters."
sidebarTitle: "Installation"
---

Install Corelens in each Node.js service you want to instrument.

```bash
npm install @varsilias/corelens
```

## Optional framework dependencies

Corelens adapters are explicit. Install a framework only when the service uses that adapter.

```bash
npm install express
npm install fastify
npm install hono
```

| Package | Needed when |
|---------|-------------|
| `express` | You register the Express metrics or tracing adapter |
| `fastify` | You register the Fastify metrics or tracing adapter |
| `hono` | You register the Hono metrics or tracing adapter |

## Package imports

```ts
import { corelens, PrometheusTextExporter } from '@varsilias/corelens';
import { ExpressMetricsAdapter } from '@varsilias/corelens/adapter';
import type { CorelensConfig } from '@varsilias/corelens/core';
```

Published entry points:

- `@varsilias/corelens`
- `@varsilias/corelens/adapter`
- `@varsilias/corelens/core`
- `@varsilias/corelens/exporters`
- `@varsilias/corelens/modules`

## Module format

The npm package is currently CommonJS with type declarations and an exports map. It works from CommonJS and from TypeScript projects using normal interop, but it is not a native dual ESM/CJS package.

## Verify installation

```bash
npm ls @varsilias/corelens
```

## Next steps

- [Quickstart](/corelens/quickstart)
- [Framework adapters](/corelens/framework-adapters/overview)
